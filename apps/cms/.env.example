# CMS stub (Strapi in Phase 2)
# Скопируйте в .env — не коммитьте секреты

PORT=1337

DATABASE_URL=postgresql://qazaqmura:qazaqmura@localhost:5432/qazaqmura

# Strapi will require these in Phase 2
APP_KEYS=dev-key-1,dev-key-2
API_TOKEN_SALT=dev-api-token-salt
ADMIN_JWT_SECRET=dev-admin-jwt-secret
JWT_SECRET=dev-jwt-secret
